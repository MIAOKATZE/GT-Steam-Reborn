package com.miaokatze.gtsgu.recipe;

/**
 * 测试用多方块机器配方注册器
 * <p>
 * 为测试用多方块机器注册配方，用于验证配方系统的集成。
 */
public class TestMachineRecipes {

    /**
     * 初始化所有测试配方
     * <p>
     * 建议在 postInit 阶段调用
     */
    public static void init() {
        // 暂不启用配方注册，保留作为框架模板
        // addAssemblerRecipes();
    }

    /**
     * 添加组装机配方
     * <p>
     * 包含一个配方：
     * 1. 测试硬币 -> 电子测试硬币（多方块机器测试）
     */
    private static void addAssemblerRecipes() {
        // 配方模板代码，暂不使用
    }
}
