# 处置汇总 — 20260822 第二轮 BUG 收束（r2）

- 生成日期：2026-08-22
- 分支基线：`fix/20260822-r2`，自 8c5b5e0（SR-COMPAT-01）起做本轮；本汇总落盘时分支 HEAD = `ca779cf`（6 个修复 commit 已全部落在分支上，另有本 docs commit）
- 泳道范围：第二轮深查（sr-survey，key `runs/r2-20260822/results/sr-survey`）BUG 9 条三态处置：可修 6 / 降级 1 / 登记不实施 1 / 信息 1
- OPT 波追加（20260822 同日，sr-opt-editor 执行）：自 docs 基线 `0766206` 起落地第二轮 OPT **批①（热身零风险）与批③（GUI 批）** 共 5 个 refactor commit，见第 7 节；批②④⑤⑥待后续波
- OPT 第二波追加（20260822 同日，sr-opt-editor2 执行）：自 `f27992b`（批①③ docs 收束）起落地 **批②（loader 配方批 O2-01/A03）与批④（节点族批 O2-10 吸收 O2-09 + A05）** 共 5 个 refactor commit（c3eb184/c5a2a14/62be7b7/d5c8552/29a8caf），见 7.4/7.5 节；批⑤⑥待后续波
- OPT 第三步追加（20260822 同日，sr-opt-editor3 执行）：自 `24771a4`（批②④ docs 收束）起落地 **批⑤第一步 O2-07 批 3（双枢纽 17 处 require 收敛）** 共 2 个 refactor commit（`484297d`/`cd5f96b`），见 7.6 节；批⑤ A04 吸收步、O2-07 二阶段与批⑥蓝图就绪待后续轮
- OPT 第四步追加（20260822 同日，sr-lite-1 接续执行）：自 `c6bf913`（批⑤第一步 docs 收束）起落地 **批⑤后续步 A04-H1~H4 双枢纽战役收尾（吸收 O2-11/12/13）+ O2-B02 + O2-16** 共 6 个 refactor commit（`cdec691`/`c8335b0`/`9a56166` 为上一执行者已完成代码、`38784fe`/`a8252b5`/`0d552a3` 为本波接续），见 7.7 节；**O2-02 本体条目随 A04 落地处置完毕**，O2-07 二阶段与批⑥待后续轮
- 基线文档：`plan/全局调查-疑似BUG-第二轮.md`（B2-01–B2-09）
- 以下 commit/文件清单/行号均为 2026-08-22 落盘前实读复核值（git log/diff --stat 亲核）

---

## 0. 统一验证

- SR 纪律逐条执行：每个修复 commit 前均跑 `./gradlew spotlessApply`（exit 0）→ `./gradlew build`（成功）→ `git status` 自审 → 显式 `git add` 指定文件 → commit。
- 终态复核：HEAD `ca779cf` 上 `./gradlew clean build` **BUILD SUCCESSFUL**（1m 21s，33 tasks）。备注：首次 clean 因 3 个残留 Gradle 守护进程文件锁（`build/rfg/recompiled_minecraft-1.7.10.jar` 无法删除）失败，`./gradlew --stop` 后复跑即绿——环境性文件锁，非代码问题；后续终段 runClient 冒烟前如遇 clean 失败可先 `--stop`。
- 既有良性警告 1 条不变：`MTERockBreakerRecipeBuilderMixin.java:25 @Overwrite is missing javadoc comment`（B2-08 登记项，本轮未触碰 mixin）。
- **本轮零 mixin 文件改动**（`git diff 8c5b5e0..HEAD --name-only | grep mixin` = 0），零 api/compat 文件改动——按纪律不触发「触碰 mixin 后必须启动冒烟」前置，runClient 冒烟由主代理终段统一处理。
- 聚合改动面：8 files changed，+53/−70（净减 17 行，其中 B2-05 删 36 行死代码是净减主体）。

---

## 1. 可修 6 条（commit 已落，逐项 git show 核对）

### 1.1 73d7e23 — fix(gtsr): [SR-B2-01] 水族缓存节点读数六处 CAPACITY 改 getRealCapacity() 跟随档位

- 文件：`common/machine/base/MTEWaterCacheNode.java`、`MTEReinforcedWaterCacheNode.java`、`MTEOverpressureWaterCacheNode.java`，3 files，+6/−6。
- 修法一句话：getInfoData 六处 `formatNumber(CAPACITY)` → `formatNumber(getRealCapacity())`，与蒸汽族三变体完全同构；容量档（5%/1%）下 Scanner/传感器卡读数自动跟随实际容量，S4「读数自动跟随档位」注释由假转真。
- 锚点复核：MTEWaterCacheNode :155/:168、MTEReinforcedWaterCacheNode :158/:171、MTEOverpressureWaterCacheNode :165/:178 原位命中（注意两处缩进不同：20 空格与 16 空格各一）。
- 冒烟待办：水缓存节点容量档 5%/1% 时 Scanner/传感器卡读数 = 满额×档%，与 GUI 罐容量一致。

### 1.2 454d3db — fix(gtsr): [SR-B2-02] 巨型轮机蒸汽实扣 long 化，消除 UIV/UMV 临界档每 tick int 截断虚省

- 文件：`common/machine/MTEMegaSteamTurbineArray.java`，1 file，+20/−12。
- 修法一句话（登记定稿方案）：`depleteSteamByType(SteamType, long)` / `outputCoolingProduct(SteamType, long)` 形参与 `remaining` 升 long；:1120/:1121 改传未钳制 long 局部量 `steamConsumption`；`mSteamConsumption` 字段保 int 仅作显示/NBT 旧档兼容（:1528 `setInteger` 不动）；`Math.min(fs.amount, remaining)` 结果恒 ≤ fs.amount，`(int)` 回收无损失；蒸馏水走 `condenseSteam(long)` 全精度，int 基输出 API（冷却蒸汽/次级蒸汽 FluidStack）按饱和值 `saturatedAmount` 钳制。
- 触发面：仅 STEAM/SH_STEAM（euPerL=1）tier≥11 + 临界(×5) + 功率 10 档——门控要求存量 ≥10B 但每 tick 实扣原被钳 2.147B（虚省 ≈4.7 倍）；致密族（euPerL=1000）消耗恒远低于 int max，不受影响；EU/t 输出无影响（mFullBaseEUt long 路径既有）。
- 冒烟待办：UIV/UMV + 普通蒸汽 + 临界 + 功率 100% + 4 叠层 + ME 仓备 >10B——实扣速率应与 GUI「蒸汽输出」词条一致。

### 1.3 bd6d210 — fix(gtsr): [SR-B2-03] /gtsr Tab 补全三档移位回正，usage 与回显补 nature/nullplus

- 文件：`common/commands/GTSRCommand.java`，1 file，+14/−5。
- 修法一句话：`addTabCompletionOptions` 三档各回正一位——`==5`（duration 位）只补 `NA`；`==6`（special 位）补 `0/null/onlypull/nullplus/nature`；`==7`（color 位）补 `VALID_COLORS` 16 色名。顺手项并入：类 javadoc 与 `getCommandUsage` 用法串补 `nullplus|nature`（nature 附一行语义说明）；spawn 回显 special 分支补 `ATTRIBUTE_NATURE(-4) → "nature"`（原显示裸数字 -4）。
- 冒烟待办：逐位敲 Tab——第 5 位 NA、第 6 位 0/null/onlypull/nullplus/nature、第 7 位 16 色名；`/gtsr singularity 10 1 1 600 nature` 回显 special nature。

### 1.4 48211f0 — fix(gtsr): [SR-B2-04] UnitFormatUtil 口径注记事实修正（纯注释）

- 文件：`common/util/UnitFormatUtil.java`，1 file，+8/−7（纯 javadoc，无代码/输出格式变化）。
- 修法一句话：按 REF 裁决证据（GTNHLib-0.11.24 Constants.java:7-11 全十进制 BigDecimal K/M/B/T/Q、NumberFormatUtil.java:103-112 后缀无 G）修正原 SR-BUG-03 注记的事实错误——「SI 的 G=giga…两套 G 相差 1000 倍」不成立，GTNHLib 全链路不输出 G 后缀；真实差异仅 10^12 档字母（本类 G vs GTNHLib T，同值仅字母不同），10^9 档两套均为 B 同值同字母。B/G 常量行内注释同步改写；迁移收益结论降级（O2-14 维持 P3 中期不升级）为该修正的直接推论。
- 冒烟待办：无行为验证项（纯注释抽查）。
- wiki 债：见第 5 节（2 处，待 wiki 同步波）。

### 1.5 0932dc1 — fix(gtsr): [SR-B2-05] 删除 absorbRays 死方法，absorbScan 注记改为真实口径

- 文件：`common/blocks/TileRunawaySingularity.java`，1 file，+4/−39（净删 36 行含死方法 32 行 + 原 4 行 javadoc）。
- 修法一句话（深查推荐修法 a）：删 `absorbRays`（全库 grep 零调用亲证，本轮删后全库仅存 absorbScan javadoc 中的历史说明文字）；`absorbScan` javadoc 由「超大范围时外层区域由射线机制覆盖」改为真实口径「>48 格的方块不吸收（性能上限，避免每 4 tick 全量立方体遍历卡顿）；仅实体路径 handleEntities 使用全范围 effRange」。
- 可选项裁量登记：深查附带的「（可选）setParams 上限收 48」**本轮不实施**——会将 /gtsr range 49-128 的实体全范围行为一并收窄，属玩家可见行为面缩减且无 BUG 依据，超出「删死代码+注释对齐」的登记修法主体；如后续要做并入行为调整批次单独评审。
- 触发面复核：机器侧失控奇点全部非吸收型（-1/-2/-3，range 8-14）+ 自然生成 8-16，方块吸收路径仅 /gtsr 吸收型 attribute 可达，删除死代码零运行时行为变化。
- 冒烟待办：/gtsr range 60 吸收型——48 格内方块照常吸收、实体全 60 格牵引（与修复前运行时行为一致，验证删除无回归）。
- wiki 债：若失控奇点机制页有引用「射线辅助吸收」表述，待 wiki 同步波排查（未单列，见第 5 节注）。

### 1.6 ca779cf — fix(gtsr): [SR-B2-06] 聚合器螺丝刀 javadoc 改写对齐 v1.10.55 实况

- 文件：`common/machine/MTECrustMatterAggregator.java`，1 file，+1/−1（一行 javadoc）。
- 修法一句话：:1745 过时 javadoc「进入时清空过滤/定向集合并重建矿池」改写为「不清空过滤/定向配置，仅强制刷新矿池并重置奇点模式，见 toggleDirectionalMode」，与 :845-865 实现真相（两表独立保留、显式清表入口为 clearCurrentModeConfig :867-875）对齐。
- 冒烟待办：无（纯注释；螺丝刀行为本身无变化）。

---

## 2. 降级 1 条（登记不实施，按深查结论并入后续批次）

### 2.1 B2-07 — UndergroundOilHelper.getFluidAmount 死代码

- 三态：降级（信息级，无行为修复项）。
- 深查结论：`UndergroundOilHelper.java:240-257` getFluidAmount 全库零外部调用亲证（读路径 v1.10.77 起走 GT5U 直调，见 MTEVeinSteamPyrolyzer.java:353/:377）；CachedReflection 失败短路语义与模块缺席场景核验通过，无崩溃面。
- 处置：**本轮不动代码**，与 O2-15 死代码批（8 个死方法）同窗口顺手删除或加「当前无调用方，留作诊断 API」javadoc；实施前按纪律重跑零引用 grep。
- **已落地（20260822 OPT 波）**：随 `c860a20` [SR-O2-15] 删除（删前零引用 grep 重跑亲证，见 7.2.1）。

---

## 3. 登记不实施 1 条（维持升级必查清单）

### 3.1 B2-08 — mixin 六处 @Overwrite

- 深查复核：全库 @Overwrite 恰 6 处，行号与 T3 登记逐处一致——MTEHatchInputMixin.java:45、MTEHatchInputBusMixin.java:54、MTEHatchSteamBusInputMixin.java:29、MTEHatchSteamBusOutputMixin.java:21、SteamHatchElementOutputBusMixin.java:47（remap=false 字符串 targets 指 GT++ 匿名类 $2，最高漂移风险位）、MTERockBreakerRecipeBuilderMixin.java:24；MTESteamMultiBaseMixin.java:244 仍为注释；GTNL 门控（GTSRMixinPlugin.java:22/:35-41）正常。
- `mixins.gtsr.json "required": true`（:2）亲证——「触碰任一 mixin 后必须启动冒烟」纪律依据成立。
- 处置：**本轮零 mixin 改动**（`git diff 8c5b5e0..HEAD` 名单 0 个 mixin 文件），不触发冒烟前置；SR-BUG-07 备案前提全部成立，维持「升级必查清单」不动。

---

## 4. 信息 1 条（无动作）

### 4.1 B2-09 — SR-OPT-02 模板上提等价性

- 登记已做 4b41c69 逐变体 diff 复核：drain 速率常量/tooltip 行序/数量结构等价；getInfoData 有意未上提（历史差异原样保留，正是本轮 B2-01 收敛掉的读数差异）。遗留动作仅终段冒烟第 4 项（tooltip 行序六变体实机对照），无代码动作。

---

## 5. wiki 债 3 处（本轮不修，统一标注「待 wiki 同步波」）

以下 3 处 wiki 页面携带本轮已修正/已回正口径的旧表述，按任务包口径不在本轮修（wiki 仓独立、走后续同步波），此处仅登记：

1. `MIAO_Wiki_For_GTNH/mods/gtsr/api/unit-format-utils.md:22-23`（B2-04 镜像）——「G 后缀与 SI 前缀 giga(=1e9) 语义冲突」表述需按 48211f0 修正后的口径改写（GTNHLib 为十进制 K/M/B/T/Q 无 G 档，差异仅 1e12 档字母），并同步 O2-14 迁移优先级降级结论。
2. `MIAO_Wiki_For_GTNH/crossmod/gtsr-gtnhlib-number-format.md:24`（B2-04 镜像）——「G=1e12 与 SI 前缀 giga 冲突…GTNHLib SI 风格」同源事实错误，与上一条同批修正。
3. `MIAO_Wiki_For_GTNH/mods/gtsr/core/singularity-command.md:18/:37`（B2-03 镜像）——:18 用法串缺 `nullplus|nature`；:37 记录的是修复前三档移位的 Tab 补全口径（「第 5 参补 NA/0/null/onlypull/nullplus/nature；第 6 参补 16 色名」），需按 bd6d210 回正后口径更新。

注：B2-05 的「射线辅助吸收」表述如在失控奇点机制页（runaway-singularity-tile.md 等）出现，可在同步波顺手排查；未单列计入 3 处。

---

## 6. 终段冒烟清单（主代理统一 runClient 处理）

1. B2-01：水缓存节点容量档 5%/1% 读数 = 满额×档%（Scanner/传感器卡）。
2. B2-02：UIV/UMV+普通蒸汽+临界+功率 100%+4 叠层+ME 仓备 >10B——实扣速率与 GUI「蒸汽输出」词条一致（不再虚省）。
3. B2-03：/gtsr 逐位 Tab（第 5 位 NA / 第 6 位 special 关键词 / 第 7 位 16 色名）+ nature 回显。
4. B2-05：/gtsr range 60 吸收型——48 格内吸收、实体 60 格牵引（与修复前运行时一致，删死代码零回归）。
5. B2-09 遗留：六变体 tooltip 行序实机对照。
6. 本轮未触碰 mixin（required=true），无 mixin 冒烟强制项。

---

## 7. 第二轮 OPT 批次处置（r2-opt：批①②③④已落地 + 批⑤双枢纽战役全部落地，批⑥待后续轮）

- 生成日期：2026-08-22（sr-opt-editor：批①热身 + 批③ GUI；sr-opt-editor2：批② loader 配方 + 批④ 节点族；sr-opt-editor3：批⑤第一步 O2-07 批 3 双枢纽 require 收敛；sr-lite-1：批⑤后续步 A04-H1~H4 + B02 + O2-16 接续收尾）
- 代码基线：批①③ 自 `0766206`（BUG 收束 docs commit）起 5 commit 至 `cf6ae02`；批②④ 自 `f27992b`（批①③ docs 收束）起 5 commit 至 `29a8caf`；批⑤第一步 自 `24771a4`（批②④ docs 收束）起 2 commit（`484297d` SteamHub + `cd5f96b` WaterHub）；批⑤后续步 自 `c6bf913`（批⑤第一步 docs 收束）起 6 commit 至 `0d552a3` + 本 docs commit
- 登记文档：`plan/全局调查-优化建议-第二轮.md`（O2-01~O2-16 + 附篇A O2-A01~A05 + 附篇B O2-B01~B05）；锚点快核基线 = sr-survey OPT 26 条快核（25 成立）
- **批①③未触碰 GTSRRecipeLoader / 双枢纽（MTESteamHubArray、MTEWaterHubArray 的结构校验段）/ 触碰窗口池（轮机 O2-04、B04②、O2-08 二步）；批②④按任务包独占 GTSRRecipeLoader 与节点族，双枢纽/轮机/触碰窗口池零触碰（`git diff f27992b..29a8caf --name-only` 亲证 0 命中），全波零 mixin、零 api/compat 改动**
- **批⑤第一步（O2-07 批 3）只做 17 处 require 收敛：触碰面 = 双枢纽 checkMachine 结构校验段 + 新增 structure 包助手类 GTSRStructureChecks，未做任何 A04/H 批抽象搬运（「禁止 H 批与 require 同 commit」纪律遵守）；A04 吸收步与 O2-07 二阶段留后续轮**
- **批⑤后续步（A04 战役）全程单执行者串行：触碰面 = 双枢纽两文件镜像段 + 新增 base/MTEHubArrayBase 与 util/HubBindingUtil + DrillingHub/CompartmentBase/FilteredCacheNode/RemoteWorkerNode 吸收点 + 四 hatch 与 IHubArray/IHubController 接口面；零 mixin、零 api/compat 触碰；NBT 键名与网络 tag 字段逐字保留；H 批与吸收步各自独立 commit（禁同 commit 纪律遵守）**

### 7.1 统一验证

- SR 纪律逐条执行：每个 commit 前 `./gradlew spotlessApply`（exit 0）→ `./gradlew build`（BUILD SUCCESSFUL）→ `git status` 自审 → 显式 `git add` → commit；终态 `cf6ae02` 上 build 复跑全绿。
- 一次中途编译失败（O2-06 首版：子类静态 `open` 与基类实例 `open` 签名冲突，javac 4 错误）——基类实例方法改名 `openGui` 后复绿，属实现内往返，最终 diff 无残留。
- 聚合改动面（5 commit）：`git diff 0766206..cf6ae02 --stat` = 25 files，+244/−487（净减 243 行，死代码删除与样板收敛为主体；新增 4 个公共类文件 BlocksGTSR / ScrollKeepingListWidget / AbstractGTSRPosUiFactory / OreEntryInfo）。
- 第二波（批②④，sr-opt-editor2）同样逐条执行：每 commit 前 spotlessApply（exit 0）→ build（BUILD SUCCESSFUL）→ git status 自审 → 显式 git add → commit；终态 `29a8caf` 上 build 复跑全绿。三次中途编译错误（R3 DSM 引用误挂 / A05 孤儿 @Override / 水族钩子漏插）均当批修复，终态无残留。聚合改动面：`git diff f27992b..29a8caf --stat` = 15 files，+2,103/−2,261（**净减 158 行**；门面 1,713→69、节点族 1,718→1,333，新增 recipes/ 包 7 文件为搬迁主体非增量）。
- 第三波（批⑤第一步，sr-opt-editor3）同样逐条执行：两 commit 前 spotlessApply（exit 0）→ build（BUILD SUCCESSFUL，仅存量 mixin @Overwrite javadoc 警告）→ git status 自审 → 显式 git add → commit；终态 `cd5f96b` 上 build 复跑全绿，中途零编译错误。聚合改动面：`git diff 24771a4..cd5f96b --stat` = 3 files，+46/−36（新增助手类 27 行；17 处样板每处 5 行块→4 行块净 −17，import 增删相抵）——纯收敛零行为变化。
- 第四波（批⑤后续步，sr-lite-1）同样逐条执行：六 commit 各自 spotlessApply（exit 0）→ build（BUILD SUCCESSFUL）→ git status --porcelain 自审 → 显式 git add（禁 add -A）→ commit；终态 `0d552a3` 上 build 复跑全绿。一次中途编译错误（B02 首版：DrillingNode/MinerNode 三处 getBoundHub 具体型赋值漏改，javac 3 错误）——调用点补 instanceof 收窄后复绿，属实现内往返，终态无残留。聚合改动面：`git diff c6bf913..0d552a3 --stat` = 17 files，+1,703/−2,107。

### 7.2 批①（热身零风险）——4 条全部处置

#### 7.2.1 c860a20 — [SR-O2-15] 删除 8 个零引用死方法（−140 行，纯删除）

- 文件/方法（删前逐个重跑零引用 grep 亲证，SteamHub:1160 / WaterHub:935 / 锅炉机侧 :572 的同名活方法未触碰）：`MTESingularityDrillingHub` teleportPlayerToNodeLegacy（含 ~50 行块注释死实现）+ findSafeTeleportHeight + hasChipInstalled（−83 行）；`MTEVoidCrustSteamBorerOLD` getStatusText（−16）；`MTESingularityMinerNode` isNaturalOre（−13）；`MTELargeGeothermalSteamBoiler` hasSteamOutputHatch（−4）；`MTELargeGeothermalSteamBoilerGui` hasOverheatChip（−6）；`UndergroundOilHelper.getFluidAmount`（−18，**B2-07 降级项按登记并入此批**）。
- 登记表 8 方法中 absorbRays（TileRunawaySingularity）已由 BUG 波 `0932dc1` [SR-B2-05] 先行删除，本批删其余 7 个 + B2-07 的 1 个 = 8 个。
- import 复核：6 文件删后均无孤立 import（EntityPlayerMP/OreInfo/GTSRItemList/CHUNK_GET_AMOUNT 等仍有活引用）；零行为面。
- 冒烟待办：无（纯删除；DrillingHub 传送/芯片门控均走 HubTeleportUtil 与既有活方法）。

#### 7.2.2 751cc16 — [SR-O2-B05] 失控奇点方块引用反转 + 两个注册小环清理

- 新建 `common/blocks/BlocksGTSR`（静态持有者，blocks 包声明、BlockLoader.initBlocks 写入一次、三方只读）；`BlockLoader.blockRunawaySingularity` 公有字段删除。
- 跨包读取点 9 处全部换 `BlocksGTSR.runawaySingularity`（TileRunawaySingularity ×2 setBlock/自检、4 台奇点机器结构元素 ×1、MTESingularityMachineBase ×3）——登记口径 10 处中 1 处位于 BUG 波已删的 absorbRays 内，实删 9 处；import 图复测：machine / machine.base / blocks → loader 反向边**归零**（环 #3/#5/#8 消除）。
- `CreativeTabManager` 自持 `Logger LOG = LogManager.getLogger("gtsr")`（日志 tag 与 GTSteamReborn.LOG 同值），去 `GTSteamReborn` import——register→main 反向边**归零**（环 #9 消除）。
- 登记复核记录 5 的「GTSteamReborn.proxy 死字段」审查证伪结论遵守：proxy 五处生命周期活调用**未触碰**。
- 冒烟待办：失控奇点机器成形判块（4 台奇点机器 + 基类三处）与 /gtsr 生成方块路径行为不变；创造模式物品栏初始化日志正常输出。

#### 7.2.3 2ddf8f5 — [SR-O2-05] ScrollKeepingListWidget 双定义上提公共顶层类

- 新建 `common/gui/widget/ScrollKeepingListWidget`（构造注入 `IntSupplier` 读 + `IntConsumer` 写宿主 `listScrollValue`）；MTECacheHubStatusGui / MTESingularityHubStatusGui 两侧私有内部类（22 行 × 2）删除，各留 1 行构造调用（`() -> listScrollValue, value -> listScrollValue = value`）。
- 服务 4 个界面不变（SteamHub/WaterHubStatusGui 为 CacheHubStatusGui 薄子类）；postResize 恢复一次 + dispose 写回滚动偏移行为逐字保留。净 −7 行（去重 + 新文件头部）。

#### 7.2.4 O2-09 水族容量口径——已随 BUG 波落地，本轮零重复改动

- 深查结论「O2-09 = B2-01」复核成立：六处 `formatNumber(getRealCapacity())` 在当前 HEAD 原位（MTEWaterCacheNode :155/:168、MTEReinforced :158/:171、MTEOverpressure :165/:178），由 `73d7e23` [SR-B2-01] 落地，本波不重复改动（登记「若 O2-10 同窗口做本项被吸收」——O2-10 属批④后续波）。

### 7.3 批③（GUI 批）——2 条全部处置

#### 7.3.1 7c0730b — [SR-O2-06] GUI 工厂四胞胎抽 AbstractGTSRPosUiFactory 泛型基类

- 新建 `common/gui/AbstractGTSRPosUiFactory<M extends MetaTileEntity>`：构造参数化四差异点（GUI id / 机器 Class / GUI 构造 `Function` / 异常文案显示名），承载 openGui 服务端守卫（EntityPlayerMP + FakePlayer 判空）、canInteractWith 64 格、writeGuiData-readGuiData xyz 三连。
- 四工厂缩为薄注册（INSTANCE + 私有构造 + 静态 open 委托 `INSTANCE.openGui`）；调用方 4 处（DrillingHub:807 / SteamHub:1226 / WaterHub:1214 / Aggregator:1742）与 CommonProxy 注册点 :108/:111/:113/:116 **零改动**（静态 open 签名保持）。
- 行为等价面：异常文案四条逐字不变（is a %s! 模板 + 原显示名）；序列化字节流逐位相同（writeVarIntToBuffer xyz 三连原样）；`GuiManager.open(this, ...)` 与原 INSTANCE 等价（单例私有构造）。331 行 → 4×~30 薄类 + 1 基类（净 −122 行）。
- 冒烟待办：四界面打开（空手+潜击钻井枢纽 / 手持终端右击三台机器）、64 格距离内可交互、越界拒绝。

#### 7.3.2 cf6ae02 — [SR-O2-B01-2] OreEntryInfo 下放 common/api/gui 顶层 DTO

- `MTECrustMatterAggregator.OreEntryInfo` 公有内部类（机器类兼任 GUI 数据 DTO 宿主，B01① 最大环 #1 的内类边）上提为 `common/api/gui/OreEntryInfo`；机器与 ConfigGui 共同 import。
- 落点裁量：登记给出「common/gui（或 api）」两案，取 **api 侧**（common/api/gui 新包，与 common.api.progress「machine/gui/mixin 三方共用」分层同构）——若落 common/gui 会新增 machine→gui import 边，与 B01① 终态「machine 包 0 条 common.gui import」直接冲突；B01①（注册表 + 16 处替换）留后续波。
- 类体逐字搬迁（字段序/两构造器不变），序列化读写（ConfigGui readOreInfo/writeOreInfo :746/:759）不动，零行为变化。

### 7.4 批②（loader 配方批）——O2-01/A03 全部处置（3 commit，sr-opt-editor2）

- 蓝图：登记 §附篇A O2-A03 六组复核表（25 register 方法计数修正成立）；落地按 A03-R1/R2/R3 三批独立 build+commit。
- 等价性证据（每批 python 逐块比对 + build 全绿）：25 个 register 方法体迁移**逐字未动**（block-by-block byte-identical 全 True）；run() 派发表 25 行的调用顺序、组名字符串、行内注释**逐字不动**，仅方法引用前缀 `GTSRRecipeLoader::` → 域类前缀（正则对照 f27992b 顺序全等）；配方注册顺序 = run() 调用顺序，零语义变化（无重平衡、try-catch 粒度不变、DisplayRecipes 类内排列与派发序不一致属登记预期）。
- 口径裁量两处（登记 A03 3.2 字面不可编译，按意图落地）：①「log/warn/safeRegister 留门面」——门面与 recipes/ 域类不同包，域类方法体的 log/warn 调用跨包不可见；落为 log/warn 随 get×3/hasNull/filterNulls 一并单源到 `RecipeLoaderUtils`（[GTSR-Recipe] 前缀单源），safeRegister 错误隔离留门面（意图保留）。②「域类方法 package-private static」——门面方法引用跨包不可达，落为 public static。
- 新增 `loader/recipes/` 包 7 文件 1,875 行：RecipeLoaderUtils 70 / ProcessingMachineRecipes 487 / NodeAndHubRecipes 282 / MultiblockMachineRecipes 569 / HatchRecipes 179 / DisplayRecipes 201 / MiscRecipes 87；门面 `GTSRRecipeLoader` 1,713 → **69 行**（仅 run() 派发表 + safeRegister + 类 javadoc）。外部引用零改动（CommonProxy `new GTSRRecipeLoader().run()` 不变，全库唯一外部引用亲证）。

#### 7.4.1 c3eb184 — [SR-A03-R1] Utils 上提 + 组一 ProcessingMachineRecipes

- 组一 7 方法（CokeOven/SiemensMartin/Ammonia/AirCompressor/AtmosphericCentrifuge/Chip/Catalyst，登记行段 :145-589）迁出 + 工具六件套上提 RecipeLoaderUtils；门面 1,713→1,216。中途无编译错误。

#### 7.4.2 c5a2a14 — [SR-A03-R2] 组二 NodeAndHubRecipes + 组三 MultiblockMachineRecipes（Assembler 七段）

- 组二 5 方法（CacheNode/TinyPlanet/HubTerminal/NodeNBTClear/Node）与组三 2 方法（MultiblockWorkbench/MultiblockAssembler）迁出；原 404 行 Assembler 单方法按登记注释块切 **7 个私有段方法**（装配线机器/GTUDK 机器/轮机与阵列/升级芯片/超压节点/奇点仓四件套/存储与输入仓），段调用序 = 原方法体行序（注册顺序零变化）；workbench 方法体逐字。门面 1,216→460。

#### 7.4.3 62be7b7 — [SR-A03-R3] 组四/组五/组六迁出 + 门面收壳

- HatchRecipes（Hatch/DistilledWaterHatch）、DisplayRecipes（6 展示配方）、MiscRecipes（Implosion/ReinforcedBrickBlastFurnace/LegacyConversion）迁出，门面收壳 **69 行**。一次中途编译错误：DSM 派发引用误挂 MiscRecipes（登记归组五），改回 DisplayRecipes 后复绿（终态无残留）。

### 7.5 批④（节点族批）——O2-10 吸收 O2-09 + A05 全部处置（同窗口 2 commit，sr-opt-editor2）

- 前提核实：O2-09 六处水族 `formatNumber(getRealCapacity())` 已随 BUG 波 `73d7e23` [SR-B2-01] 落地（当前 HEAD 亲证原位），本波按登记「O2-10 同窗口做则吸收」执行吸收版。
- 族行数：MTEFilteredCacheNode 671→784（+113，承载两套 final 模板与钩子）；六变体 164-182 → **78-100**（合计 1,047→549）；族合计 1,718→1,333（**净 −385**；登记预估 −470 差异为预估未计基类 javadoc/抽象钩子样板行，方向一致）。

#### 7.5.1 d5c8552 — [SR-O2-10] getInfoData 六份上提基类 final 模板（吸收 O2-09）

- 基类新增 `public final String[] getInfoData()`（方法体与六变体被删实现逐字等价，python 归一化比对 True）；六变体各删 ~32 行 + 无用 import 清理（formatNumber ×6、Steam 变体的 EnumChatFormatting/StatCollector）。7 files +39/−200。O2-09 随模板单源闭环——同类「六份手抄」漂移不可再发生。

#### 7.5.2 29a8caf — [SR-A05] 流体门控四件套 + getTexture 模板上提（7 files +146/−374）

- 基类：`isFluidStackAllowed(FluidStack)` protected 钩子（默认 = 非空栈 + isFluidAllowed(Fluid) 谓词）+ final 四件套 `isFluidInputAllowed`/`fill(FluidStack,boolean)`（门控与 S4 温和保留合一，负/零 space 拒新入语义逐字保留）/`fill(ForgeDirection,...)`/`allowPutStack`（GTUtility.getFluidForFilledItem 中转不变）+ final `getTexture` 模板 + 三个抽象材质钩子 getTopTexture/getBottomTexture/getSideTexture。
- 等价性亲证：六变体 fill 路径谓词与默认钩子**逐字等价**——蒸汽族单名/双名归结 isFluidAllowed 同谓词；超压蒸汽三路 OR（isAnySteamFluidType + GTModHandler.isAnySteam/isSuperHeatedSteam）经 GTNHLib/GT5U 源码亲证仅依赖 fluidID，与 isFluidAllowed 同谓词；水族三变体 S5 放宽后恒真（登记预判的 water 双名/OPW 恒真两处 FluidStack 级覆写点在当前树已不存在，零覆写落地）；方向版 fill 双重门控幂等（与原变体→基类→父类链一致）。
- 变体侧：删四件套 + private static isXxxFluid ×6 + getTexture ×6 + onRightclick 空转发 ×6；纹理钩子按族落地（青铜/钢 TextureFactory.of 三静态、超压 getCasingTextureForId(CASING_INDEX)）；NBT（mTransferRatePercent/mCapacityLimitPercent/gtsr.hubPos 反转极性/save-setItemNBT 三处对称）零触碰。
- 实现内往返 3 次（孤儿 @Override 行、水族钩子漏插 + import 误删、S4 javadoc 顺序），均 git restore/修复后终态 build 全绿，最终 diff 无残留。

### 7.6 批⑤第一步（双枢纽战役第一步）——O2-07 批 3 全部处置（2 commit，sr-opt-editor3）

- 蓝图：深查-checkMachine样板收敛 §2.1 定稿设计——`GTSRStructureChecks.require(cond, errors)` 助手（structure 包、静态导入、不吞控制流）+ 复位动作与提前 return 留调用处；依赖链「先 require 后抽象」，本步为 O2-02/A04 双枢纽基类抽象的硬前置。
- 新增 `common/structure/GTSRStructureChecks`（27 行，与 GTSRRedstoneHatchLimitError 同包）：cond 为 false 时向 errors 追加 UNKNOWN_STRUCTURE_ERROR 并返回 false；javadoc 标注仅限结构校验语境（玩家可读错误属二阶段 requireLang，不在本类）。
- 17 处迁移全部为形态 B（add + issueTileUpdate + return）：失败条件经德摩根取反为成功条件入参（逐处布尔等价人工核对），复位动作与提前 return 逐字保留；两文件 StructureErrorRegistry import 删净（StructureError 留 checkMachine 签名），checkPiece 调用、探高循环、纹理更新与收尾 issueTileUpdate 零触碰。
- 等价性证据：逐处 diff 亲审（每处仅「条件行 + add 行」→「新条件行」）+ 终态 build 全绿；调用点残余 75→58（双枢纽 17 处清零，其余属 O2-07 批 1/2 与二阶段范围；另助手类自身单点实现 1 处，全库 grep 计 59）。

#### 7.6.1 484297d — [SR-O2-07] GTSRStructureChecks.require 助手落地 + MTESteamHubArray 8 处收敛

- 8 处：堆叠层数 >0 / 四 tier 全正（casing+pipe+gear+frame）/ 三 tier 一致 / 三段互斥（tier1 禁耐压+超压、tier2 禁普通+超压、tier≥3 禁普通+耐压）/ tier≥3 需超压 / 三类单元总数 >0；2 files +36/−17（含助手类 27 行）。

#### 7.6.2 cd5f96b — [SR-O2-07] MTEWaterHubArray 9 处收敛（批 3 双枢纽收官）

- 9 处：堆叠层数 >0 / 三 tier 全正（casing+pipe+frame）/ 两 tier 一致 / 三段互斥（镜像蒸汽枢纽，字段为 HubUnitCount 族）/ tier1 需普通 / tier2 需耐压 / tier≥3 需超压；1 file +10/−19。

### 7.7 批⑤后续步——A04-H1~H4 双枢纽战役收尾 + O2-B02/O2-16 吸收全部处置（6 commit）

- 接续背景：H1~H3 三 commit（`cdec691`/`c8335b0`/`9a56166`）由上一执行者完成但未及 docs 收束即中断；其工作树遗留 `MTEHubArrayBase.java` **+28 行半成品**（FRAME_UNBOUND_FACING/mStackCount/mClientFluidName 字段与一批 import、无使用点）经实读判定为 A04-H4 展示层起步残留（与附篇A §4.4 批次表 H4 行完全对应），按任务包预授权路径「蓝图下一步清晰可完成→按蓝图补完」处理：**未回滚**，三字段随 `38784fe` 补完为使用点。
- 全程单执行者串行（双枢纽 3,541 行回归敏感窗口纪律）；每 commit build 全绿；零行为变化（纯结构吸收/去重）。

#### 7.7.1 前置三 commit（上一执行者，代码已落地）

| commit | 内容 | diff |
|---|---|---|
| `cdec691` | [SR-A04] H1 基类骨架 + 无状态镜像段（resolve 族/getCacheNodeListTag/状态 UI 远程操作组/绑定调试/芯片判定/螺丝刀/getNodeTransferRate/BoundCacheNode） | 3 files +434/−697 |
| `c8335b0` | [SR-A04] H2 绑定流模板 + HubBindingUtil.consumeSteamEntangledSingularities（吸收 O2-11 三枢纽绑定流/O2-12 逐字双份，DrillingHub 第三份消耗段同收） | 5 files +347/−479 |
| `9a56166` | [SR-A04] H3 传输/NBT/服务 tick 模板（transferWithBoundNodes+transferOneNode 钩子、save/load 骨架+绑定列表双侧格式钩子、onPostTick 模板；吸收 O2-13：createHubPosTag 收敛 6 文件 12 处写块） | 8 files +296/−271 |

#### 7.7.2 `38784fe` — [SR-A04-H4] 展示层与 checkMachine 模板上提基类（A04 收官）

- 上提：registerIcons 与 FRAME_UNBOUND_FACING 单源、getDescriptionData/onDescriptionPacket（写值与 storedFluidNameForSync 同源）、getTexture（familyFallbackFluid 钩子 steam/WATER）、tierCasingTextureIndex 助手、drawTexts 模板（guiLangPrefix/unitsPerStack/getTotalUnitCount/bufferLangKey/addFamilySyncers 五钩子）、getInfoData 模板（tierDisplayText 钩子）、createTooltip 模板（tooltipStructureDims/addFamilyStructureInfo 钩子——蒸汽侧多 4 行 casingExactly、蓄水侧 counts 行差异保留）、checkMachine 骨架 reset→BASE→探高→CAP→tier 一致→三段互斥→贴图→issueTileUpdate（detachHatchControllers/resetFamilyStructureState/checkCapPiece/areTiersComplete/areTiersConsistent/checkUnitRules/updateHatchTextures 七钩子；piece 序列与 tier 字段集差异参数化——蒸汽有 CAP+Gear 四轴、蓄水无）。
- 零行为要点：失败分支 issueTileUpdate 时序逐字保留（每处失败恰一次更新后返回，跳过换装与末次更新）；**Waila 等级行历史差异零行为保留**——蒸汽侧 getInfoData 仅两档（tier≥3 显示 bronze 文案）与蓄水侧三档不一致，经 tierDisplayText 覆写维持现状（潜在 BUG 候选，建议另立条目处置，本波不动）。
- diff：3 files +568/−629。

#### 7.7.3 `a8252b5` — [SR-O2-B02] 枢纽控制器窄接口化，base→machine 反向引用切断

- 新增 `IHubController extends IHubArray`（isFormed/getTotalCapacity/getStoredFluidAmount/getStoredFluidStack），MTEHubArrayBase 改 implements、getStoredFluidAmount 升 public；四 hatch mController 改型 IHubController，receiveSteam/receiveWater→receiveFluid、extractSteam/extractWater→extractFluid（既有委托覆写语义不变）、getSteamStored/getWaterStored→getStoredFluidAmount（同值读数）；`common.machine.base` 对 `common.machine` 的 import **归零**（登记目标结构达成）。
- IHubArray 增 `isReadyForRemoteRegistration()`：钻井枢纽覆写 = isMachineRunning()（成形且允许运作），双枢纽恒 true——原 RemoteWorkerNode 的钻井 instanceof 门语义逐字保持；getBoundHub() 返回类型改 IHubArray（原零调用方），DrillingNode/MinerNode 三处调用点以 instanceof 收窄回具体类（调用面需钻井专有方法），语义不变。GT5U addToMachineList hatch element 未泛型化（登记护栏遵守）。
- diff：13 files +79/−35。

#### 7.7.4 `0d552a3` — [SR-O2-16] 四 hatch getInfoData 双行块静态助手单源

- `GTSRUtils.formatHubTankLine(stored, capacity)`：GREEN 存量 L + RESET + YELLOW 容量 L 输出行逐字等价，四份枢纽 I/O hatch getInfoData 双行块改调助手。注：四胞胎 GT 父类不同（Input×MTEHatchInput / Output×MTEHatchOutput）无法抽登记设想的 MTEHubHatchBase 中间基类，按方案要点的备选路径「静态格式化助手消 4 份」落地。diff：5 files +19/−36。

#### 7.7.5 O2-02 本体条目处置宣告

- **O2-02（双枢纽钩子基类拆分）= 附篇A O2-A04 施工图实现，随 A04-H1~H4 六 commit 全部落地宣告处置完毕**；登记锚点的同窗口吸收面全部完成：O2-11 绑定流三份、O2-12 consume 逐字双份、O2-13 hubPos 写块（机器侧+节点侧 12 处）、O2-B02 四 hatch 改型与 RemoteWorkerNode 接口化、O2-16 读数块单源。O2-07 二阶段 requireLang（17 处键随基类单点写）依赖链就绪，留批⑥前窗口执行。

#### 7.7.6 终态行数与预估对照

| 文件 | 战役前 | 终态 |
|---|---|---|
| MTESteamHubArray | 1,801 | 979 |
| MTEWaterHubArray | 1,740 | 953 |
| base/MTEHubArrayBase（新增） | — | 1,182 |
| util/HubBindingUtil（新增） | — | 65 |
| base/IHubController（新增） | — | 24 |
| 合计 | 3,541 | 3,203 |

- 聚合口径：`git diff c6bf913..0d552a3 --stat` = 17 files，+1,703/−2,107（含 DrillingHub/CompartmentBase/FilteredCacheNode/RemoteWorkerNode 吸收面与四 hatch）。
- 预估对照：附篇A §4.5 预估净 −1,680 为「基类 ~950 + 两薄类 ~910」的理想上沿口径；实际基类 1,182、两 hub 残余 979/953，差异主因是结构 static{} 定义体/hatch 枚举与 addToMachineList 族/tier 静态 getter/receive-extract 存储族/construct 族/族 NBT 段均属登记「留子类」面且实际占比高于蓝图假设。零行为约束下未强行深化。

### 7.8 批次状态总览（第四波后更新）

| 批次 | 条目 | 状态 |
|---|---|---|
| 批⑤ 双枢纽战役 | ① O2-07 批 3（17 处 require）② A04-H1~H4（O2-02/A04 吸收 O2-11/12/13）③ B02/O2-16 吸收 | ①②③ **全部落地**（`484297d`/`cd5f96b` + `cdec691`~`38784fe` + `a8252b5`/`0d552a3`）；O2-07 二阶段 requireLang 待批⑥前窗口 |
| 批⑥ 时机窗口 | O2-04/A01（等轮机触碰窗口）、B04②（tier getter 表驱动，等价性核对未全绿不得合入）、O2-08 第二步（等观察期）、B01①（GUI 注册表 16 处）、B03①②（api 倒置，②注意避开 GTVersionCompat 区） | 蓝图就绪待后续轮（禁并行） |

### 7.9 本波冒烟清单（并入主代理终段 runClient 统一处理）

1. 四 GUI 界面打开与 64 格交互（O2-06 工厂收敛回归）。
2. 枢纽节点状态列表滚动位置重建保持（O2-05 回归：滚动后触发面板重建不回顶）。
3. 失控奇点：4 台奇点机器成形（判块经 BlocksGTSR）+ /gtsr 生成 + 自然生成（B05 回归）。
4. 聚合器终端配置界面矿石列表数据（B01② 回归：过滤/定向/权重行显示与序列化）。
5. 创造模式物品栏初始化日志与图标（B05 CreativeTabManager 自持 Logger）。
6. 批①③波零 mixin 改动（`git diff 0766206..cf6ae02 --name-only | grep mixin` = 0），无 mixin 冒烟强制项。
7. **（批②追加）** 首次进世界 postInit 日志 25 组 `[GTSR-Recipe] Registered ... successfully.` 逐条对照（组名 25 个无缺漏）；NEI 抽查组装机（超压节点/奇点仓/芯片）、工作台（12 台机器）、展示配方页（地热/DSM/聚爆/纠缠）——A03 迁移零注册回归。
8. **（批④追加）** 六节点 info 面读数跟随容量档（5%/1% 档 = 满额×档%）——O2-10 模板回归。
9. **（批④追加）** 六节点流体门控三态：拒异种流体（蒸汽节点灌水）/GUI 输入槽容器拦截（非目标流体单元放不进）/降档后超额保留不销毁——A05 四件套模板回归。
10. **（批④追加）** 六节点外观：顶面流体窗+绑定框架层（未绑/接收/发送三态）、正面 OVERLAY_PIPE 叠加——getTexture 模板化回归。
11. **（批②④波）** 零 mixin 改动（`git diff f27992b..29a8caf --name-only | grep mixin` = 0 亲证），无 mixin 冒烟强制项。
12. **（批⑤第一步追加）** 双枢纽故意摆错结构回归：tier 混装（tier1 机身 + 耐压/超压单元）/ 堆叠层数 0 / 顶盖缺失时，控制器 GUI 结构错误列表仍报「未知结构错误」且机器保持未成形——O2-07 require 收敛零行为变化验证（错误条数与时机与迁移前一致）。
13. **（批⑤后续步追加）** 双枢纽 GUI 词条/终端进度面板/Waila 读数/tooltip 与重构前逐行一致；蒸汽枢纽 Waila 等级行 tier≥3 显示 bronze 文案的历史现状不变（H4 展示层模板回归）。
14. **（批⑤后续步追加）** 正面流体窗随存储流体切换、抽干回退族默认（蒸汽/水）、正面框架层渲染正常（registerIcons 单源回归）；四枢纽 I/O hatch Waila 读数行格式不变（O2-16 助手回归）；hatch 在枢纽未成形时独立运行、重新成形恢复注入（B02 mController 改型回归）。
